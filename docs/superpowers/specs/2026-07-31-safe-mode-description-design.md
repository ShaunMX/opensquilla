# 安全模式说明文案设计

## 目标

把执行模式菜单中“安全模式”下方的说明压缩为一行，让不了解“沙箱”、文件规则或命令规则的用户也能立即看懂。

## 最终文案

> 在保护环境中运行，遇到危险操作会先询问你。

## 范围

- 只修改 `chat.composer.runModeSafeDesc` 的用户可见文案及对应回归测试。
- 保持现有执行模式布局、交互和安全策略不变。
- 同步维护项目现有的六种语言，避免翻译键回退或语言间含义漂移。

## 验收

- 简体中文说明为上述最终文案。
- 文案在当前执行模式弹窗宽度内保持一行。
- 文案与组件、国际化和架构检查均通过。
